# SiteLink Integration Configuration
# Add these environment variables to enable SiteLink Web Edition integration

# Required: SiteLink API Credentials
SITELINK_API_URL=https://your-sitelink-server.com/api
SITELINK_USERNAME=your_api_username
SITELINK_PASSWORD=your_api_password

# Optional: Site and Corporate Configuration
SITELINK_SITE_ID=your_site_id
SITELINK_CORPORATE_CODE=your_corporate_code

# Integration Settings
SITELINK_SYNC_ENABLED=true
SITELINK_AUTO_SYNC_INTERVAL=300000  # 5 minutes in milliseconds
SITELINK_REAL_TIME_SYNC=true

# Development/Production
NODE_ENV=development

# Instructions:
# 1. Contact SiteLink support to get API access for your Web Edition account
# 2. Create a corporate API user with "API All Reports" permissions
# 3. Add security policy for the sites you want to integrate
# 4. Update the values above with your actual credentials
# 5. Set SITELINK_SYNC_ENABLED=true to enable automatic syncing

# API Endpoints (for reference):
# - Units: GET /api/units
# - Tenants: GET /api/tenants  
# - Transactions: GET /api/transactions
# - Payments: POST /api/payments
# - Site Info: GET /api/sites
# - Availability: GET /api/units/{id}/availability

# Integration Features:
# - Two-way sync between SummitOS and SiteLink
# - Real-time availability checking
# - Payment processing integration
# - Unit and tenant management sync
# - Transaction history synchronization